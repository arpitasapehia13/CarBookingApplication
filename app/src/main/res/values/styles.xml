<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="Theme.CabBooking" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- AppBar Overlay -->
    <style name="Theme.CabBooking.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
    </style>

    <!-- Popup Overlay -->
    <style name="Theme.CabBooking.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light">
    </style>

    <style name="RatingBar" parent="Widget.AppCompat.RatingBar">

        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:numStars">5</item>
        <item name="android:stepSize">1</item>
        <item name="android:progressDrawable">@drawable/custom_ratingbar</item> <!-- Optional: Custom drawable -->
    </style>

</resources>
